title: string

order:
  type: integer
  order: true

content:
  type: document-markdown
  render:
    generateHeaderId: true
    autolink: blank
    typography: true

uid:
  type: document-uid
  path: true

summary:
  type: markdown
  optional: true
